# PyTorch (CUDA 12.6)
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0
--extra-index-url https://download.pytorch.org/whl/cu126

# 核心训练库（关键！）
transformers>=4.36.0
accelerate>=0.24.0
peft>=0.10.0
trl>=0.11.0  # 较新版本，包含GRPO支持
bitsandbytes>=0.43.0

# 数据处理
datasets>=2.18.0
jsonlines>=4.0.0

# 工具
tqdm
numpy>=1.24.0
scipy
pandas
sentencepiece
protobuf<=3.20.3  # 高版本与部分旧模型不兼容
regex
rich  # trl依赖的模块

# Reward相关
bert-score
seqeval

# NER医学模型
spacy